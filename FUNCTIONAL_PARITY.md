# 功能设计对标清单

本文件以 `../phantun-client-win` 当前源码和随包配置为唯一基线。验收的是使用者能够看到和依赖的功能设计：可配置内容、启动入口、数据转发、会话处理、超时和退出后的结果。macOS 不能因系统或实现方式不同而少任何一项功能，也不新增需要用户理解的配置概念。

| Windows 客户端功能设计 | macOS 客户端对应 | 验证方式 |
| --- | --- | --- |
| 随包 `phantun-client.json` 的 local、remote、ipv4_only、四个 TUN 字段及 routes 示例 | 同字段、同值、同结构 | Rust 单元测试 |
| 缺省配置中的 local、remote、ipv4_only、四个 TUN 字段及 routes | 同默认值、同缺省填充 | Rust 单元测试 |
| 启动时读取当前目录配置；--config 重载；CLI 覆盖配置 | 同一读取与覆盖顺序 | Rust 单元测试与命令行测试 |
| -c/-l/-r/-4/--tun-local/--tun-peer | 同名称、短参数和说明，二进制名称同为 phantun-client | 二进制 --help 对照 |
| 任意配置的 UDP 监听 IP:端口 | 同地址解析、绑定、回包和报错行为 | 本机 UDP 集成测试 |
| 远端域名解析与 --ipv4-only | 同样选择第一个满足条件的地址 | Rust 单元测试与联调 |
| 自动选择当前网络下可用的本机源地址 | 同一用户结果：不需要额外填写本机公网 IP | 网络集成测试 |
| 伪 TCP 建连、双向数据和断开 | 同一会话状态和数据结果 | fake-tcp 行为测试与真实服务端联调 |
| 仅接收已配置服务端的回包 | 同一远端地址、端口边界 | 单元测试与抓包联调 |
| 多 UDP 客户端并发转发 | 与 Windows 版相同的每连接 worker 结构 | 多客户端集成测试 |
| 180 秒无流量超时与退出时 RST/清理 | 同常量、同连接生命周期 | 时钟驱动测试与联调 |
| RUST_LOG 日志、启动/异常信息 | 同级别和同类事件 | 命令行与联调 |
| tun_local、tun_peer、tun_local6、tun_peer6、routes 的 Windows 兼容语义 | 保持配置兼容，不将它们变成 macOS 专有功能 | 配置解析测试 |

## 当前自动验收

- 配置模板、缺省值、命令行参数及覆盖顺序。
- 单个 UDP 客户端的建连、双向数据和正常断开。
- 两个 UDP 客户端同时转发、回包只返回各自客户端。
- 空闲超时后的关闭和下一次 UDP 数据触发新会话。
- 服务端断开后的关闭和下一次 UDP 数据触发新会话。

静态与本机会话测试只证明代码和协议状态机，不替代真实网络验收。最终验收还必须在真实 Phantun Server、Intel Mac 和 Apple Silicon Mac 上验证 WireGuard 握手、双向数据、同时连接、断开恢复和清理。
